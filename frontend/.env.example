# PresenceOS Frontend — Environment Variables
# Copy this file to .env.local and fill in your values

# === API ===
NEXT_PUBLIC_API_URL=http://localhost:8000
NEXT_PUBLIC_APP_URL=http://localhost:3000

# === NextAuth (if used) ===
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-nextauth-secret-here

# === Sentry — Error Monitoring ===
# Get from https://sentry.io → Project Settings → Client Keys
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_ORG=presenceos
SENTRY_PROJECT=presenceos-frontend

# === Mixpanel — Analytics ===
# Get from https://mixpanel.com → Project Settings → Token
NEXT_PUBLIC_MIXPANEL_TOKEN=

# === Crisp — Live Chat ===
# Get from https://crisp.chat → Settings → Website Settings → Website ID
NEXT_PUBLIC_CRISP_WEBSITE_ID=

# === hCaptcha — Anti-bot ===
# Get from https://hcaptcha.com → Sites → Site Key
NEXT_PUBLIC_HCAPTCHA_SITEKEY=
